# Replace with your actual swap resume offset if using hibernation:
# Calculate with: btrfs inspect-internal map-swapfile -r /path/to/swapfile
# KERNEL_CMDLINE[default]+=" resume=/dev/mapper/root resume_offset=<RESUME_OFFSET>"
